<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.developeralfa.calendoer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".allTasks"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".Welcome_firstrun"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name=".DetailsForm" />
        <activity android:name=".Decider">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sorted" />
        <activity android:name=".threeimp_tut" />
        <activity
            android:name=".notification_tut"
            android:label="App"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".recurring_tut" />
        <activity android:name=".priority" />
        <activity android:name=".taskForm" />
        <activity android:name=".taskDetails"></activity>
    </application>
</manifest>
